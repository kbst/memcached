{
	"variables": [
		{
			"path": "$.items..metadata.name",
			"type": "text",
			"prefix": "memc-",
			"title": "name your memcached cluster"
		},
		{
			"path": "$.items..metadata.namespace",
			"type": "text",
			"value": "default",
			"title": "namespace to deploy cluster in"
		},
		{
			"path": "$.items[?(@.kind == 'Memcached')].replicas",
			"type": "number",
			"value": 2,
			"min": 2,
			"title": "# of replicas"
		},
		{
			"path": "$.items[?(@.kind == 'Memcached')].limit_cpu",
			"type": "text",
			"value": "100m",
			"title": "# of CPU cores"
		},
		{
			"path": "$.items[?(@.kind == 'Memcached')].limit_memory",
			"type": "text",
			"value": "32Mi",
			"title": "RAM per replica"
		}
	]
}
